---
import "../styles/footer.css";

import logo from "../assets/footer/logo.svg";
import servicesIcon from "../assets/footer/services-a-la-personne.png";
import { Image } from "astro:assets";
import { site } from "../lib/site-data";

const navigation = [
  { label: "Accueil", href: "/" },
  { label: "Comment ça marche ?", href: "/#comment-ca-marche" },
  { label: "Formules", href: "/formules" },
  { label: "À propos", href: "/a-propos" },
  { label: "Avance immédiate", href: "/avanceimmediate" },
  { label: "Journal", href: "/journal" },
  { label: "Zone d'intervention", href: "/cuisinier-a-domicile-oise" },
  { label: "FAQ", href: "/faq" },
];

const conditions = [
  { label: "Mentions légales", href: "/mentions-legales" },
  { label: "CGV", href: "/cgv" },
  { label: "Politique de confidentialité", href: "/politique-de-confidentialite" },
];

const annee = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer__top">
    <img src={logo.src} alt="Le Cuistologue" class="footer__logo" />

    <div class="footer__brand">
      <h2 class="footer__tagline">Le goût du lien</h2>
      <p class="footer__description">
        Des repas préparés directement chez vous, par un professionnel, pour retrouver le plaisir de bien manger sans passer vos soirées en cuisine.
      </p>
      {site.social.length > 0 && (
        <div class="footer__social">
          {site.social.map((reseau) => (
            <a href={reseau.url} aria-label={reseau.label} target="_blank" rel="noopener noreferrer">
              {reseau.label}
            </a>
          ))}
        </div>
      )}
      {/* Réseaux sociaux masqués tant que les vraies URL ne sont pas
          renseignées dans site.social (src/lib/site-data.ts). */}
    </div>

    <nav class="footer__col footer__col--nav">
      <h3 class="footer__col-title">Navigation</h3>
      <ul>
        {navigation.map((item) => (
          <li><a href={item.href}>{item.label}</a></li>
        ))}
      </ul>
    </nav>

    <nav class="footer__col footer__col--conditions">
      <h3 class="footer__col-title">Conditions</h3>
      <ul>
        {conditions.map((item) => (
          <li><a href={item.href}>{item.label}</a></li>
        ))}
        <li><a href="#" id="footer-gerer-cookies">Gérer mes cookies</a></li>
      </ul>
    </nav>

    <div class="footer__col footer__col--contact">
      <h3 class="footer__col-title">Contact</h3>
      <ul>
        <li><a href={site.phone.href}>{site.phone.display}</a></li>
        <li><a href={`mailto:${site.email}`}>{site.email}</a></li>
      </ul>
      <p class="footer__address">
        Basé à {site.location.city} ({site.location.region})
      </p>
      <p class="footer__zone">
        Zone d'intervention<br />
        {site.areas.map((a) => a.name).join(" · ")}
      </p>
      <Image src={servicesIcon} alt="Services à la personne" class="footer__services-icon" />
    </div>
  </div>

  <div class="footer__bottom">
    <p>© {annee} Le Cuistologue. Tous droits réservés.</p>
    <p>Fait avec ❤️ par Antoine Bruneau</p>
  </div>
</footer>
